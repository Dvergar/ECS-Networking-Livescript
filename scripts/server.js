// Generated by LiveScript 1.2.0
(function(){
  'use strict';
  var x$, position, y$, position2, output;
  x$ = position = new CPosition;
  x$.x = 42;
  x$.y = 42;
  y$ = position2 = new CPosition;
  y$.x = 12;
  y$.y = 12;
  console.log(position);
  output = new dcodeIO.ByteBuffer();
  output.append(
  position.encode());
  console.log("offset " + output.offset);
  output.writeInt16(100);
  output.flip();
  console.log("postflip offset " + output.offset);
  console.log("postflip length " + output.length);
  console.log("buffer " + output.toColumns());
  console.log("buffer " + output.toColumns());
  console.log("buffer " + output.toColumns());
  console.log("postflip offset " + output.offset);
  console.log("postflip length " + output.length);
  console.log(output.readInt16());
  net.listen('god', 'localhost', 9000);
  net.onOpen = onOpen;
  function onOpen(conn){
    console.log('onOpen');
    net._send(position);
    return net._send(position2);
  }
  function dummy(){
    return console.log;
  }
  setInterval(function(){
    return em.fixedUpdate(dummy);
  }, 1000 / 60);
}).call(this);
